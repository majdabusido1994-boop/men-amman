[build]
  base    = "frontend"
  command = "npm ci && npx expo export --platform web"
  publish = "dist"

[build.environment]
  EXPO_PUBLIC_API_URL = "https://men-amman-api.onrender.com/api"
  NODE_VERSION        = "18"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
